ategories prompt = [
    hey make changes only in categories section images like decrease the widths of images and add the hover animation on each relative image like if i hover on an image then current hovered image should increase width without shrink image and make this animation smoothly without glitch and don't change horizontal scoll animation and after this don't change and disturb other code now do this properly and cleanly
]